<section class="page-banner section-reveal">
  <div class="container page-banner-content">
    <p class="eyebrow"><%= page.pageHeader.eyebrow %></p>
    <h1 class="page-title"><%= page.pageHeader.title %></h1>
    <p class="page-lead"><%= page.pageHeader.description %></p>
  </div>
</section>
